## Why

What was wrong or missing, in a sentence or two. Not a tour of the diff.

## What

## Checks

- [ ] `go test ./...` passes
- [ ] Downloads still cannot leave `uploads/` or `assets/`
- [ ] New eviction or path behaviour has a test
- [ ] No `uploads/` or `.ips.json` from a live host in the diff
